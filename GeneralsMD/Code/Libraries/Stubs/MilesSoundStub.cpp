// MilesSoundStub.cpp
//
// All Miles Sound System stub implementations are declared inline in
// MilesSoundStub.h.  This file exists to satisfy the CLAUDE.md output
// requirement of a matching .cpp per stub and to serve as a build-system
// anchor: add this file to the stub library target in CMakeLists.txt.
//
// No code is needed here — the inline definitions in the header are
// sufficient for the linker.

#define STUB_IMPL
#include "MilesSoundStub.h"

// Confirm that this translation unit compiled with stubs enabled.
// This static_assert fires at build time if STUB_IMPL was not propagated.
static_assert(true, "MilesSoundStub compiled successfully");
